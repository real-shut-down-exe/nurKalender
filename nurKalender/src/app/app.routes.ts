import { Routes } from '@angular/router';
import { KalenderComponent } from './features/kalender/kalender.component';

export const routes: Routes = [
    { path: '', component: KalenderComponent },
];